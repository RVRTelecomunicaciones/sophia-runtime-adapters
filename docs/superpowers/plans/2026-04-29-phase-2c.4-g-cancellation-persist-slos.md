# Phase 2C.4 / G — Cancellation-rate + persist-availability SLOs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship 8 cancellation-rate SLOs (per Phase 1 capability) + 1 persist-availability SLO (global) + a contract test that protects the SLI denominator derivation, then re-enable the two currently-skipped chaos scenarios so the nightly comprehensive returns to 6/6 active. Close at tag `v0.5.0`.

**Architecture:** Pure YAML + Sloth-rendered rule files + a single Go contract test in `_test.go`. No production-code changes; no metric-contract changes; no alertmanager changes. Cancellation SLOs slot into existing per-adapter SLO files; persist SLO ships in a new dedicated file. Test SLOs mirror the B6 chaos-test convention (`service: "runtime-adapters-chaos-test"`, 5m period). The two chaos scenarios skipped at `v0.4.0` (`ci-shell-hang-cancel`, `ci-persist-fail`) are unskipped against the new `ShellExecCancellationRateBurn` and `PersistAvailabilityBurn` alerts.

**Tech Stack:** Sloth 0.16.0 (SLO generator, pinned in `ops/slo/.sloth-version`); Prometheus rule files; Go 1.26 testing with OpenTelemetry `manual.Reader` for the contract test; existing `ChaosReceiptStore` for persist-fault injection.

**Spec:** `docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md`.

**Roadmap entry:** `docs/phase-2c.4-roadmap.md` §G.

---

## Bundles

- **B1** — SLO YAML additions (cancellation × 8 + persist × 1) + Sloth render + contract test (1 PR).
- **B2** — Chaos comprehensive re-enable (un-skip 2 scenarios) + CHANGELOG 0.5.0 (1 PR).
- **Closure** — manual nightly verification + tag `v0.5.0`.

Branch naming: `feat/phase-2c.4-g-bundle-1-slos-and-contract-test` and `feat/phase-2c.4-g-bundle-2-reenable-chaos`.

Cadence per bundle: single-dispatch + combined review (B1 has more files but uniform pattern; B2 is small).

---

## Bundle 1 — SLO YAML + Sloth render + contract test

Goal: ship the 9 new SLO entries (prod + test variants), regenerate the Sloth output rule files, add the contract test that protects the persist denominator, land via PR + CI green + merge.

Branch: `feat/phase-2c.4-g-bundle-1-slos-and-contract-test`.

### Task 1.1: Branch from main + sync

**Files:** none (git only).

- [ ] **Step 1: Create branch from `main`**

```bash
git checkout main
git pull
git checkout -b feat/phase-2c.4-g-bundle-1-slos-and-contract-test
git push -u origin feat/phase-2c.4-g-bundle-1-slos-and-contract-test
```

Expected: branch tracking origin, clean working tree.

### Task 1.2: Add cancellation-rate SLO to `ops/slo/shell.yaml`

**Files:**
- Modify: `ops/slo/shell.yaml` (existing — append a new SLO entry after `shell-exec-latency`)

- [ ] **Step 1: Read the file's current state**

Run: `cat ops/slo/shell.yaml | wc -l`

Expected: 58 lines (file currently ends at the close of the `shell-exec-latency` SLO).

- [ ] **Step 2: Append cancellation-rate SLO entry + PROVISIONAL header annotation**

Edit `ops/slo/shell.yaml`. Add the following block AFTER the `shell-exec-latency` SLO closes (after the existing `severity: warning` line at file end). Keep file tail newline.

Add a "Phase 2C.4 / G additions" header block at the top of the file (after the existing top header) and a new SLO entry at the bottom of `slos:`.

Final file shape after edit:

```yaml
# Calibrated — Phase 2C.2 baseline 2026-04-25 (latest-baseline.json v2).
# Compose envelope: 2 CPU / 2 GiB runtime (ADR 0008).
# Changing these targets requires re-running `make load-baseline`
# under the same or a newer-but-declared envelope.
#
# ---
# 2C.4 / G additions: shell-exec-cancellation-rate SLO is PROVISIONAL.
# Cancellation objective (99.0%) is an operator hypothesis, not yet
# calibrated against real load. Recalibrate with evidence per
# docs/load-baseline.md when production traffic gives signal.

version: "prometheus/v1"
service: "runtime-adapters"
labels:
  team: "platform"
  adapter: "shell"

slos:
  - name: "shell-exec-availability"
    objective: 99.5
    description: "shell.exec@v1 non-error execution rate (failure+timeout burn budget; cancelled+partial neutral)"
    labels:
      capability: "shell.exec@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="shell.exec@v1",status=~"failure|timeout"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="shell.exec@v1",status=~"success|failure|timeout"}[{{.window}}]))
    alerting:
      name: ShellExecAvailabilityBurn
      labels:
        capability: "shell.exec@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "shell-exec-latency"
    objective: 99.0
    description: "99% of shell.exec@v1 successful executions complete under 250ms"
    labels:
      capability: "shell.exec@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_duration_seconds_count{capability="shell.exec@v1"}[{{.window}}]))
          -
          sum(rate(runtime_adapters_execution_duration_seconds_bucket{capability="shell.exec@v1",le="0.25"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_duration_seconds_count{capability="shell.exec@v1"}[{{.window}}]))
    alerting:
      name: ShellExecLatencyBurn
      labels:
        capability: "shell.exec@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "shell-exec-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "shell.exec@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "shell.exec@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="shell.exec@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="shell.exec@v1"}[{{.window}}]))
    alerting:
      name: ShellExecCancellationRateBurn
      labels:
        capability: "shell.exec@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 3: Lint YAML (no syntax errors)**

Run: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' ops/slo/shell.yaml`

Expected: no output (silent success).

### Task 1.3: Add cancellation-rate SLOs to `ops/slo/git.yaml`

**Files:**
- Modify: `ops/slo/git.yaml`

- [ ] **Step 1: Add PROVISIONAL header block + 4 cancellation-rate SLO entries**

Insert at top of file (after existing header, before `version: "prometheus/v1"`):

```yaml
# ---
# 2C.4 / G additions: git-*-cancellation-rate SLOs are PROVISIONAL.
# Cancellation objectives (99.0%) are operator hypotheses, not yet
# calibrated against real load. Recalibrate with evidence per
# docs/load-baseline.md when production traffic gives signal.
```

Append 4 new SLO entries at the end of `slos:` (after the last existing SLO closes). Each follows the cancellation-rate template with capability-specific values:

```yaml
  - name: "git-status-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "git.status@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "git.status@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.status@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.status@v1"}[{{.window}}]))
    alerting:
      name: GitStatusCancellationRateBurn
      labels:
        capability: "git.status@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "git-clone-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "git.clone@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "git.clone@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.clone@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.clone@v1"}[{{.window}}]))
    alerting:
      name: GitCloneCancellationRateBurn
      labels:
        capability: "git.clone@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "git-diff-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "git.diff@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "git.diff@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.diff@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.diff@v1"}[{{.window}}]))
    alerting:
      name: GitDiffCancellationRateBurn
      labels:
        capability: "git.diff@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "git-commit-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "git.commit@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "git.commit@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.commit@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.commit@v1"}[{{.window}}]))
    alerting:
      name: GitCommitCancellationRateBurn
      labels:
        capability: "git.commit@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 2: Lint YAML**

Run: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' ops/slo/git.yaml`

Expected: no output.

### Task 1.4: Add cancellation-rate SLOs to `ops/slo/filesystem.yaml`

**Files:**
- Modify: `ops/slo/filesystem.yaml`

- [ ] **Step 1: Add PROVISIONAL header block + 2 cancellation-rate SLO entries**

Insert at top of file (after existing header, before `version: "prometheus/v1"`):

```yaml
# ---
# 2C.4 / G additions: filesystem-*-cancellation-rate SLOs are PROVISIONAL.
# Cancellation objectives (99.0%) are operator hypotheses, not yet
# calibrated against real load. Recalibrate with evidence per
# docs/load-baseline.md when production traffic gives signal.
```

Append 2 new SLO entries at the end of `slos:`:

```yaml
  - name: "filesystem-read-file-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "filesystem.read_file@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "filesystem.read_file@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.read_file@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.read_file@v1"}[{{.window}}]))
    alerting:
      name: FilesystemReadFileCancellationRateBurn
      labels:
        capability: "filesystem.read_file@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "filesystem-write-file-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "filesystem.write_file@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "filesystem.write_file@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.write_file@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.write_file@v1"}[{{.window}}]))
    alerting:
      name: FilesystemWriteFileCancellationRateBurn
      labels:
        capability: "filesystem.write_file@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 2: Lint YAML**

Run: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' ops/slo/filesystem.yaml`

Expected: no output.

### Task 1.5: Add cancellation-rate SLO to `ops/slo/http.yaml`

**Files:**
- Modify: `ops/slo/http.yaml`

- [ ] **Step 1: Add PROVISIONAL header block + 1 cancellation-rate SLO entry**

Insert at top of file (after existing header, before `version: "prometheus/v1"`):

```yaml
# ---
# 2C.4 / G additions: http-request-cancellation-rate SLO is PROVISIONAL.
# Cancellation objective (99.0%) is an operator hypothesis, not yet
# calibrated against real load. Recalibrate with evidence per
# docs/load-baseline.md when production traffic gives signal.
```

Append 1 new SLO entry at the end of `slos:`:

```yaml
  - name: "http-request-cancellation-rate"
    objective: 99.0   # PROVISIONAL — cancellation rate < 1% (G, 2C.4)
    description: "http.request@v1 caller-cancellation rate (PROVISIONAL — see file header)"
    labels:
      capability: "http.request@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="http.request@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="http.request@v1"}[{{.window}}]))
    alerting:
      name: HttpRequestCancellationRateBurn
      labels:
        capability: "http.request@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 2: Lint YAML**

Run: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' ops/slo/http.yaml`

Expected: no output.

### Task 1.6: Create `ops/slo/persist.yaml` (new prod file)

**Files:**
- Create: `ops/slo/persist.yaml`

- [ ] **Step 1: Write the new prod persist SLO file**

Full file content:

```yaml
# PROVISIONAL — initial operational hypothesis (Phase 2C.4 sub-project G).
# Persist-availability objective (99.9%) is an operator hypothesis, not
# yet calibrated against real-load data. Recalibrate with evidence under
# the load envelope (ADR 0008, docs/load-baseline.md) when production
# traffic gives signal.
# Tracked: 2C.4 sub-project G; closes at v0.5.0.

version: "prometheus/v1"
service: "runtime-adapters"
labels:
  team: "platform"

slos:
  - name: "persist-availability"
    objective: 99.9   # PROVISIONAL — persist failure rate < 0.1% (G, 2C.4)
    description: "Receipt persistence reliability across all capabilities (denominator derived per comment in total_query)"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_receipt_persist_failures[{{.window}}]))
        # Denominator derivation (D2C4G.1):
        #   total_persist_attempts =
        #     successful_persists  (every execution.total fires AFTER a
        #                           successful persist per
        #                           execute_service.go RecordExecution
        #                           semantics post-B8/B9)
        #   + failed_persists       (receipt.persist.failures counter)
        # Mathematically EXACT, not approximate. Protected by
        # Test_RecordExecution_OnlyAfterPersistSuccess in
        # internal/application/services/execute_service_test.go.
        # If that test ever fails, this denominator is wrong — fix the
        # ordering in execute_service before touching this SLI.
        total_query: |
          (
            sum(rate(runtime_adapters_execution_total[{{.window}}]))
            +
            sum(rate(runtime_adapters_receipt_persist_failures[{{.window}}]))
          )
    alerting:
      name: PersistAvailabilityBurn
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 2: Lint YAML**

Run: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' ops/slo/persist.yaml`

Expected: no output.

### Task 1.7: Mirror cancellation SLOs in `ops/slo/test/{shell,git,filesystem,http}.yaml`

**Files:**
- Modify: `ops/slo/test/shell.yaml`
- Modify: `ops/slo/test/git.yaml`
- Modify: `ops/slo/test/filesystem.yaml`
- Modify: `ops/slo/test/http.yaml`

The test variants use `service: "runtime-adapters-chaos-test"` (not `runtime-adapters`). All SLI selectors stay the same; only `service:` differs. For each test file, append the same cancellation-rate SLO entries as their prod counterpart (Tasks 1.2-1.5), without the PROVISIONAL header (test files are already short-window-rendered for chaos use).

- [ ] **Step 1: Append shell-exec-cancellation-rate SLO to `ops/slo/test/shell.yaml`**

Append at end of `slos:`:

```yaml
  - name: "shell-exec-cancellation-rate"
    objective: 99.0
    description: "shell.exec@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "shell.exec@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="shell.exec@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="shell.exec@v1"}[{{.window}}]))
    alerting:
      name: ShellExecCancellationRateBurn
      labels:
        capability: "shell.exec@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 2: Append 4 git-*-cancellation-rate SLOs to `ops/slo/test/git.yaml`**

Append at end of `slos:` — 4 entries identical to Task 1.3 except for `service: "runtime-adapters-chaos-test"` already at top of file (no PROVISIONAL annotation needed; description: "(chaos test variant)" suffix instead of "(PROVISIONAL — see file header)").

```yaml
  - name: "git-status-cancellation-rate"
    objective: 99.0
    description: "git.status@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "git.status@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.status@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.status@v1"}[{{.window}}]))
    alerting:
      name: GitStatusCancellationRateBurn
      labels:
        capability: "git.status@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "git-clone-cancellation-rate"
    objective: 99.0
    description: "git.clone@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "git.clone@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.clone@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.clone@v1"}[{{.window}}]))
    alerting:
      name: GitCloneCancellationRateBurn
      labels:
        capability: "git.clone@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "git-diff-cancellation-rate"
    objective: 99.0
    description: "git.diff@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "git.diff@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.diff@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.diff@v1"}[{{.window}}]))
    alerting:
      name: GitDiffCancellationRateBurn
      labels:
        capability: "git.diff@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "git-commit-cancellation-rate"
    objective: 99.0
    description: "git.commit@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "git.commit@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.commit@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="git.commit@v1"}[{{.window}}]))
    alerting:
      name: GitCommitCancellationRateBurn
      labels:
        capability: "git.commit@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 3: Append 2 filesystem-*-cancellation-rate SLOs to `ops/slo/test/filesystem.yaml`**

Append at end of `slos:`:

```yaml
  - name: "filesystem-read-file-cancellation-rate"
    objective: 99.0
    description: "filesystem.read_file@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "filesystem.read_file@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.read_file@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.read_file@v1"}[{{.window}}]))
    alerting:
      name: FilesystemReadFileCancellationRateBurn
      labels:
        capability: "filesystem.read_file@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning

  - name: "filesystem-write-file-cancellation-rate"
    objective: 99.0
    description: "filesystem.write_file@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "filesystem.write_file@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.write_file@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="filesystem.write_file@v1"}[{{.window}}]))
    alerting:
      name: FilesystemWriteFileCancellationRateBurn
      labels:
        capability: "filesystem.write_file@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 4: Append 1 http-request-cancellation-rate SLO to `ops/slo/test/http.yaml`**

Append at end of `slos:`:

```yaml
  - name: "http-request-cancellation-rate"
    objective: 99.0
    description: "http.request@v1 caller-cancellation rate (chaos test variant)"
    labels:
      capability: "http.request@v1"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_execution_total{capability="http.request@v1",status="cancelled"}[{{.window}}]))
        total_query: |
          sum(rate(runtime_adapters_execution_total{capability="http.request@v1"}[{{.window}}]))
    alerting:
      name: HttpRequestCancellationRateBurn
      labels:
        capability: "http.request@v1"
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 5: Lint all 4 test YAMLs**

Run:
```bash
for f in ops/slo/test/{shell,git,filesystem,http}.yaml; do
  python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" "$f" || echo "FAIL: $f"
done
```

Expected: no output (all files parse cleanly).

### Task 1.8: Create `ops/slo/test/persist.yaml` (test variant)

**Files:**
- Create: `ops/slo/test/persist.yaml`

- [ ] **Step 1: Write the test persist SLO file**

Full file content (mirror of `ops/slo/persist.yaml` with `service: "runtime-adapters-chaos-test"` and shorter description):

```yaml
# Test SLO spec for the chaos comprehensive nightly run.
# Mirror of ops/slo/persist.yaml with service: "runtime-adapters-chaos-test"
# to avoid sloth_slo collision when both prod and test rules load in the
# same Prometheus instance.

version: "prometheus/v1"
service: "runtime-adapters-chaos-test"
labels:
  team: "platform"

slos:
  - name: "persist-availability"
    objective: 99.9
    description: "Receipt persistence reliability (chaos test variant; denominator derived per comment in total_query)"
    sli:
      events:
        error_query: |
          sum(rate(runtime_adapters_receipt_persist_failures[{{.window}}]))
        # Denominator derivation: see ops/slo/persist.yaml for full
        # rationale (D2C4G.1). Same SLI selector logic; this file
        # only differs in service: label so test rules don't collide
        # with prod rules in the chaos compose stack.
        total_query: |
          (
            sum(rate(runtime_adapters_execution_total[{{.window}}]))
            +
            sum(rate(runtime_adapters_receipt_persist_failures[{{.window}}]))
          )
    alerting:
      name: PersistAvailabilityBurn
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
```

- [ ] **Step 2: Lint YAML**

Run: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' ops/slo/test/persist.yaml`

Expected: no output.

### Task 1.9: Render Sloth output rule files

**Files:**
- Modify (regen): `ops/prometheus/generated/{shell,git,filesystem,http}.yaml`
- Create (regen): `ops/prometheus/generated/persist.yaml`
- Modify (regen): `ops/prometheus/generated/test/{shell,git,filesystem,http}.yaml`
- Create (regen): `ops/prometheus/generated/test/persist.yaml`

- [ ] **Step 1: Verify Sloth version pin**

Run: `cat ops/slo/.sloth-version`

Expected: `0.16.0` (or whatever the pin currently is).

- [ ] **Step 2: Render prod rules**

Run: `make sloth-generate`

Expected: per-file rendered output under `ops/prometheus/generated/` includes a new `persist.yaml` and updated `{shell,git,filesystem,http}.yaml` carrying the new cancellation-rate burn-rate alerts.

- [ ] **Step 3: Render chaos test rules**

Run: `make chaos-render-rules`

Expected: per-file rendered output under `ops/prometheus/generated/test/` includes a new `persist.yaml` and updated `{shell,git,filesystem,http}.yaml`.

- [ ] **Step 4: Verify idempotency (two-pass render)**

Run: `make chaos-render-rules-check`

Expected: silent success (no diff against committed files after a second render). If diff appears, re-run `make chaos-render-rules` and re-run the check.

- [ ] **Step 5: Spot-check rendered alert names**

Run: `rg 'name: ShellExecCancellationRateBurn|name: PersistAvailabilityBurn' ops/prometheus/generated/{,test/}*.yaml | head -20`

Expected: at least 2 matches per alert name (Sloth emits multi-window burn-rate alerts as multiple rule entries — page-tier + ticket-tier).

- [ ] **Step 6: Verify all 8 cancellation alert names present in prod rendered output**

Run:
```bash
for alert in ShellExecCancellationRateBurn GitStatusCancellationRateBurn GitCloneCancellationRateBurn \
             GitDiffCancellationRateBurn GitCommitCancellationRateBurn \
             FilesystemReadFileCancellationRateBurn FilesystemWriteFileCancellationRateBurn \
             HttpRequestCancellationRateBurn; do
  count=$(rg -c "name: $alert" ops/prometheus/generated/*.yaml | awk -F: '{sum+=$NF} END {print sum+0}')
  echo "$alert: $count occurrences"
done
```

Expected: each alert name reports >= 2 occurrences (Sloth emits 2 alert tiers per SLO).

### Task 1.10: Add contract test `Test_RecordExecution_OnlyAfterPersistSuccess`

**Files:**
- Modify: `internal/application/services/execute_service_test.go` (append a new test function)

- [ ] **Step 1: Inspect existing manual.Reader usage in obs tests**

Run: `rg -n 'manual.NewReader|manual.Reader|sdkmetric.NewMeterProvider' internal/ | head -10`

Expected: at least one match in `internal/infrastructure/obs/exemplar_test.go` showing the manual-reader pattern. Use that as the model for the contract test.

- [ ] **Step 2: Append the contract test to `execute_service_test.go`**

Add at end of file:

```go
// Test_RecordExecution_OnlyAfterPersistSuccess is a CONTRACT TEST for the
// metric semantics that ops/slo/persist.yaml depends on.
//
// The persist-availability SLI derives its denominator as:
//
//	total_persist_attempts =
//	  execution.total{any status} (= successful persists)
//	  + receipt.persist.failures  (= failed persists)
//
// This is exact ONLY IF execution.total is incremented strictly AFTER
// a successful persist. If a future refactor reorders execute_service
// to emit RecordExecution before or in parallel with persist, the
// denominator goes wrong silently and the SLO produces meaningless
// ratios.
//
// This test forces both branches:
//
//	(1) persist succeeds  → execution.total += 1, persist.failures += 0
//	(2) persist fails     → execution.total += 0, persist.failures += 1
//
// If either assertion fails, the SLI in ops/slo/persist.yaml is no
// longer valid — fix the ordering in execute_service.go BEFORE
// touching the SLI.
//
// Spec: docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md §7
func Test_RecordExecution_OnlyAfterPersistSuccess(t *testing.T) {
	t.Parallel()

	t.Run("persist_succeeds_increments_execution_total_only", func(t *testing.T) {
		// Arrange: set up a fixture with a non-failing receipt store, a
		// manual.Reader-backed MeterProvider, and the standard execute
		// service.
		harness := newPersistContractHarness(t, persistMode_OK)

		// Act: execute one request through the service.
		harness.executeOne(t)

		// Assert: snapshot the metrics. execution.total{capability=...}
		// must have incremented by exactly 1; persist.failures by 0.
		snap := harness.snapshotMetrics(t)
		require.Equal(t, int64(1), snap.executionTotal,
			"ops/slo/persist.yaml total_query is no longer valid: "+
				"expected execution.total += 1 after successful persist, got %d. "+
				"Fix execute_service.go ordering before touching the SLI.",
			snap.executionTotal)
		require.Equal(t, int64(0), snap.persistFailures,
			"ops/slo/persist.yaml total_query is no longer valid: "+
				"expected persist.failures += 0 after successful persist, got %d.",
			snap.persistFailures)
	})

	t.Run("persist_fails_increments_persist_failures_only", func(t *testing.T) {
		// Arrange: chaos receipt store returning ErrChaosPersistFailure.
		harness := newPersistContractHarness(t, persistMode_FAIL)

		// Act: execute one request; expect a 5xx-shaped error from the service.
		err := harness.executeOneExpectingErr(t)
		require.Error(t, err, "execute must return error when persist fails")

		// Assert: persist.failures += 1, execution.total += 0. This is the
		// invariant load-bearing on the SLI denominator derivation in
		// ops/slo/persist.yaml — see test docstring above.
		snap := harness.snapshotMetrics(t)
		require.Equal(t, int64(0), snap.executionTotal,
			"ops/slo/persist.yaml total_query is no longer valid: "+
				"expected execution.total += 0 when persist fails, got %d. "+
				"RecordExecution must NEVER fire before persist succeeds. "+
				"Fix execute_service.go ordering before touching the SLI.",
			snap.executionTotal)
		require.Equal(t, int64(1), snap.persistFailures,
			"ops/slo/persist.yaml total_query is no longer valid: "+
				"expected persist.failures += 1 when persist fails, got %d.",
			snap.persistFailures)
	})
}

// persistContractMode controls whether the receipt store fails on persist.
type persistContractMode int

const (
	persistMode_OK persistContractMode = iota
	persistMode_FAIL
)

// persistContractHarness is the test-only setup that wires a manual.Reader
// MeterProvider to the runtime metric Registry, builds an ExecuteService
// with a real or chaos-injected receipt store, and exposes a single
// snapshotMetrics() method that returns the two counters this contract
// test cares about. Implemented as a minimal harness (no goroutines,
// no concurrency) — TDD for one happy-path execution per subtest.
//
// Implementation: instantiate sdkmetric.NewManualReader, wire it into a
// MeterProvider, register obs.Registry against it, build ChaosReceiptStore
// (FAIL mode) or normal store (OK mode) per the persistContractMode
// argument, build ExecuteService with the standard test fixture, and
// hold them all in a struct.
//
// snapshotMetrics() calls reader.Collect to flush the latest cumulative
// counter values, then walks the resulting metric data to extract the
// two counters by name (runtime_adapters.execution.total and
// runtime_adapters.receipt.persist.failures — OTel names, not Prom).
type persistContractHarness struct {
	// Fields filled by newPersistContractHarness; details are
	// implementation-side — write them when implementing.
	// At minimum: *manual.Reader, *services.ExecuteService, context.Context.
}

// persistContractSnapshot holds the two counter readings.
type persistContractSnapshot struct {
	executionTotal  int64
	persistFailures int64
}

// newPersistContractHarness builds the test fixture. See type docstring.
func newPersistContractHarness(t *testing.T, mode persistContractMode) *persistContractHarness {
	t.Helper()
	// Implementation: see existing exemplar_test.go for the manual.Reader
	// pattern; existing chaos integration tests for the ChaosReceiptStore
	// wiring; existing execute_service_test.go fixtures for the standard
	// service builder. Combine.
	t.Skip("implement using the patterns from internal/infrastructure/obs/exemplar_test.go + " +
		"internal/infrastructure/chaos/chaos_receipt_repository_test.go + " +
		"existing execute_service_test.go fixtures. " +
		"Test only proceeds once the harness is wired.")
	return nil
}

// executeOne drives a single request through the service. Asserts no error.
func (h *persistContractHarness) executeOne(t *testing.T) {
	t.Helper()
	t.Skip("implement together with newPersistContractHarness")
}

// executeOneExpectingErr drives a single request, returning the error.
func (h *persistContractHarness) executeOneExpectingErr(t *testing.T) error {
	t.Helper()
	t.Skip("implement together with newPersistContractHarness")
	return nil
}

// snapshotMetrics flushes the manual.Reader and returns the two
// counters by their OTel names.
func (h *persistContractHarness) snapshotMetrics(t *testing.T) persistContractSnapshot {
	t.Helper()
	t.Skip("implement together with newPersistContractHarness")
	return persistContractSnapshot{}
}
```

> Note: the harness scaffolding above contains `t.Skip` placeholders. Step 3 below replaces those with real implementations. The two-step structure (skeleton-then-implement) is intentional: it makes Step 3's diff small and reviewable in isolation, and keeps the contract-test docstring + assertions stable from the moment they're committed.

- [ ] **Step 3: Implement `persistContractHarness` methods**

Replace the `t.Skip` calls in the four helpers with real wiring. Reference patterns:

- `internal/infrastructure/obs/exemplar_test.go` — `manual.NewManualReader` + `sdkmetric.NewMeterProvider` setup.
- `internal/infrastructure/chaos/chaos_receipt_repository_test.go` — `ChaosReceiptStore` instantiation in FAIL mode.
- Top of `internal/application/services/execute_service_test.go` — find the existing fixture builder (`newServiceForTest`, `buildTestService`, or whatever it's named) that wires a clock, ID generator, repo, etc. Reuse it.

Implementation outline (do NOT copy verbatim — adapt to the existing fixture API the test file uses):

```go
func newPersistContractHarness(t *testing.T, mode persistContractMode) *persistContractHarness {
    t.Helper()

    reader := sdkmetric.NewManualReader()
    mp := sdkmetric.NewMeterProvider(sdkmetric.WithReader(reader))
    meter := mp.Meter("runtime-adapters")
    registry, err := obs.NewRegistry(meter)
    require.NoError(t, err)

    store := <existing in-memory test receipt store; e.g. newInMemoryReceiptStore()>
    if mode == persistMode_FAIL {
        store = chaos.NewChaosReceiptStore(store, chaos.PersistFailureProfile())
    }

    svc := <existing fixture builder; pass `registry` + `store`>

    return &persistContractHarness{
        ctx:      context.Background(),
        svc:      svc,
        reader:   reader,
    }
}

func (h *persistContractHarness) executeOne(t *testing.T) {
    t.Helper()
    _, err := h.svc.Execute(h.ctx, <minimal valid request>)
    require.NoError(t, err)
}

func (h *persistContractHarness) executeOneExpectingErr(t *testing.T) error {
    t.Helper()
    _, err := h.svc.Execute(h.ctx, <minimal valid request>)
    return err
}

func (h *persistContractHarness) snapshotMetrics(t *testing.T) persistContractSnapshot {
    t.Helper()
    var rm metricdata.ResourceMetrics
    require.NoError(t, h.reader.Collect(h.ctx, &rm))

    var snap persistContractSnapshot
    for _, sm := range rm.ScopeMetrics {
        for _, m := range sm.Metrics {
            switch m.Name {
            case "runtime_adapters.execution.total":
                if sum, ok := m.Data.(metricdata.Sum[int64]); ok {
                    for _, dp := range sum.DataPoints {
                        snap.executionTotal += dp.Value
                    }
                }
            case "runtime_adapters.receipt.persist.failures":
                if sum, ok := m.Data.(metricdata.Sum[int64]); ok {
                    for _, dp := range sum.DataPoints {
                        snap.persistFailures += dp.Value
                    }
                }
            }
        }
    }
    return snap
}
```

> Adapt the `<existing in-memory test receipt store>`, `<existing fixture builder>`, and `<minimal valid request>` placeholders to the actual helpers present in `execute_service_test.go`. The implementer reads the file once to find the right names; this plan does not assume them.

- [ ] **Step 4: Run the contract test**

Run: `go test -run Test_RecordExecution_OnlyAfterPersistSuccess ./internal/application/services/...`

Expected: PASS (both subtests). If FAIL, the existing `execute_service.go` ordering is already broken — STOP, do not edit the SLI, fix the ordering first per the test's failure message.

- [ ] **Step 5: Run the full execute_service test suite to verify nothing else broke**

Run: `go test ./internal/application/services/... -count=1`

Expected: PASS for all tests.

### Task 1.11: Run full lint + unit + contract suite locally

**Files:** none.

- [ ] **Step 1: Vet**

Run: `go vet ./...`

Expected: no output.

- [ ] **Step 2: Build**

Run: `go build ./...`

Expected: no output.

- [ ] **Step 3: Unit + contract tests**

Run: `go test ./... -count=1 -short`

Expected: PASS. The contract test runs in this suite (regular unit-test tag).

### Task 1.12: Commit B1 changes

**Files:** all modified above.

- [ ] **Step 1: Stage**

Run:
```bash
git add ops/slo/{shell,git,filesystem,http}.yaml \
        ops/slo/persist.yaml \
        ops/slo/test/{shell,git,filesystem,http,persist}.yaml \
        ops/prometheus/generated/{shell,git,filesystem,http,persist}.yaml \
        ops/prometheus/generated/test/{shell,git,filesystem,http,persist}.yaml \
        internal/application/services/execute_service_test.go
```

- [ ] **Step 2: Commit**

Run:
```bash
git commit -m "$(cat <<'EOF'
feat(slo): cancellation-rate per-capability + persist-availability global SLOs (B1)

Phase 2C.4 / G — first bundle.

Adds 8 cancellation-rate SLOs (1 per Phase 1 canonical capability) and
1 persist-availability global SLO. Initial objectives are PROVISIONAL:
  - cancellation rate per capability: 99.0%  (cancellation rate < 1%)
  - persist availability (global):    99.9%  (persist failure rate < 0.1%)

Cancellation SLOs land inside the existing per-adapter SLO files
(shell.yaml, git.yaml, filesystem.yaml, http.yaml). Persist SLO ships
in a new dedicated file ops/slo/persist.yaml because it's not
adapter-specific. Test variants under ops/slo/test/ mirror the prod
shape with service: "runtime-adapters-chaos-test" for the chaos canary.

Persist SLI denominator is derived from existing metrics:
  total_persist_attempts = execution.total + receipt.persist.failures
which is exact via the RecordExecution-after-persist invariant
established post-B8/B9. NO new metric counter; metric contract (R3)
remains frozen.

The derivation is protected by a contract test
(Test_RecordExecution_OnlyAfterPersistSuccess) in
internal/application/services/execute_service_test.go. The test
asserts both branches: persist OK → execution.total += 1, failures += 0;
persist FAIL → execution.total += 0, failures += 1. Failure messages
explicitly point at the SLI it protects.

No runtime Go code changes (the contract test is _test.go-only). No
metric contract change. No alertmanager change (the existing
inhibit_rules `equal: ['sloth_slo', 'capability']` clause already
handles both labeled and unlabeled SLOs — Alertmanager treats absent
capability on both source and target as equal).

Sloth-rendered burn-rate recording rules + alerts land under
ops/prometheus/generated/ (prod) and ops/prometheus/generated/test/
(chaos test, 5m period). chaos-render-rules-check passes idempotency.

Refs: docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md
      docs/phase-2c.4-roadmap.md §G
      D2C4G.1, D2C4G.2, D2C4G.3, D2C4G.5, D2C4G.6, D2C4G.7
      A2C4G.1 (contract test must be explicitly named as such)
EOF
)"
```

- [ ] **Step 3: Push**

Run: `git push`

Expected: branch updated on origin.

### Task 1.13: Open PR for B1, await CI green, merge

**Files:** none.

- [ ] **Step 1: Open the PR**

Run:
```bash
gh pr create --title "feat: Phase 2C.4 / G B1 — cancellation + persist SLOs + contract test" --body "$(cat <<'EOF'
## Summary

First bundle of sub-project G under Phase 2C.4. Adds 8 per-capability cancellation-rate SLOs + 1 global persist-availability SLO. Sloth-renders the burn-rate alerts. Adds a contract test that protects the persist SLI denominator derivation.

No runtime Go changes. No metric contract changes. No alertmanager changes.

## Files changed

| Area | Change |
|---|---|
| `ops/slo/{shell,git,filesystem,http}.yaml` | +8 cancellation SLOs (PROVISIONAL header annotation) |
| `ops/slo/persist.yaml` | NEW — global persist SLO (PROVISIONAL) |
| `ops/slo/test/*.yaml` | mirror cancellation SLOs + new persist.yaml for chaos canary |
| `ops/prometheus/generated/{,test/}*.yaml` | regenerated burn-rate alerts |
| `internal/application/services/execute_service_test.go` | +contract test `Test_RecordExecution_OnlyAfterPersistSuccess` |

## Spec / plan

- Spec: `docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md`
- Plan: `docs/superpowers/plans/2026-04-29-phase-2c.4-g-cancellation-persist-slos.md` Bundle 1

## Test plan

- [ ] `lint + unit + contract` green (the contract test runs here)
- [ ] `observability` green (sloth idempotency gate covers the new test rules)
- [ ] `chaos-canary-e2e` green (canary scenario unchanged in this bundle)
- [ ] `integration` green
- [ ] Other CI jobs green
- [ ] After merge: B2 (re-enable chaos scenarios) follows in a separate PR

Closes nothing yet — `v0.5.0` ships with B2.
EOF
)"
```

- [ ] **Step 2: Watch CI**

Run: `gh pr checks $(gh pr view --json number --jq .number) --watch`

Expected: 9/9 green (lint + unit + contract, observability, integration, chaos-canary-e2e, load ops lint, load-smoke advisory, load report schema, GitGuardian, plus e2e SKIPPED for non-main).

- [ ] **Step 3: Merge**

Run: `gh pr merge $(gh pr view --json number --jq .number) --merge --delete-branch`

Expected: branch deleted on origin; main fast-forwarded locally after `git pull`.

- [ ] **Step 4: Sync local main**

Run: `git checkout main && git pull`

Expected: local main matches origin.

---

## Bundle 2 — Chaos comprehensive re-enable + CHANGELOG

Goal: un-skip both chaos scenarios so the nightly comprehensive returns to 6/6, write the CHANGELOG 0.5.0 entry, land via PR + CI green + merge.

Branch: `feat/phase-2c.4-g-bundle-2-reenable-chaos`.

### Task 2.1: Branch from main

**Files:** none.

- [ ] **Step 1: Branch + push**

Run:
```bash
git checkout main
git pull
git checkout -b feat/phase-2c.4-g-bundle-2-reenable-chaos
git push -u origin feat/phase-2c.4-g-bundle-2-reenable-chaos
```

### Task 2.2: Update `ci-shell-hang-cancel` row in `comprehensive_test.go`

**Files:**
- Modify: `test/chaos/e2e/comprehensive_test.go`

- [ ] **Step 1: Locate the row**

Run: `rg -n 'name:\s*"shell-hang-cancel"' test/chaos/e2e/comprehensive_test.go`

Expected: one match around line 131-135.

- [ ] **Step 2: Modify the row**

Replace the existing entry (the one with `skipReason:` populated) with:

```go
		{
			name:         "shell-hang-cancel",
			profileFile:  "ci-shell-hang-cancel.yaml",
			adapterID:    "shell",
			capability:   "exec",
			version:      "v1",
			canonicalCap: "shell.exec@v1",
			payloadJSON:  `{"command":"true","args":[],"working_dir":"","env":null,"stdin":null}`,
			timeoutMs:    5000,
			useCancel:    true,
			// 50ms is long enough for the POST to reach the runtime handler
			// and start executing the shell command; short enough that we're
			// well below timeoutMs=5000 so the runtime classifies as
			// cancelled, not timeout.
			cancelAfter: 50 * time.Millisecond,
			alertName:   "ShellExecCancellationRateBurn",
			sloName:     "shell-exec-cancellation-rate",
			specFile:    "shell.yaml",
		},
```

The `skipReason:` block is removed (the field defaults to "" which means "do not skip").

### Task 2.3: Update `ci-persist-fail` row in `comprehensive_test.go`

**Files:**
- Modify: `test/chaos/e2e/comprehensive_test.go`

- [ ] **Step 1: Locate the row**

Run: `rg -n 'name:\s*"persist-fail"' test/chaos/e2e/comprehensive_test.go`

Expected: one match around line 168-181.

- [ ] **Step 2: Replace the existing skip-only entry**

Replace the entire `persist-fail` entry (the one with only `name`, `profileFile`, and `skipReason`) with:

```go
		{
			name:         "persist-fail",
			profileFile:  "ci-persist-fail.yaml",
			adapterID:    "http",
			capability:   "request",
			version:      "v1",
			canonicalCap: "", // persist SLO is global; capability label intentionally empty
			payloadJSON:  `{"method":"GET","url":"https://example.invalid/","headers":{},"expected_status":[200]}`,
			timeoutMs:    1000,
			alertName:    "PersistAvailabilityBurn",
			sloName:      "persist-availability",
			specFile:     "persist.yaml",
		},
```

### Task 2.4: Update file header comment

**Files:**
- Modify: `test/chaos/e2e/comprehensive_test.go` (top-of-file doc block, current lines 22-52)

- [ ] **Step 1: Replace the "two scenarios are included for completeness but skipped" block + Scenarios bullet**

Find the doc comment block ending at line 52 (right before `func TestChaos_Comprehensive`). Replace the contents from the line starting `// Two scenarios are included in the table for completeness but skipped` through the line ending `//	ci-persist-fail             → SKIPPED (no test SLO covers persist counter)`.

Replacement text:

```go
// All six CI scenarios are active in the suite as of v0.5.0. The two
// scenarios that shipped skipped under v0.4.0 (ci-shell-hang-cancel,
// ci-persist-fail) are now backed by the cancellation-rate and
// persist-availability SLOs introduced in Phase 2C.4 / G — see
// docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md.
//
// Compose lifecycle: ComposeUp + ComposeDown PER SCENARIO. docker compose
// fixes container env at start; the only clean way to swap the chaos
// profile is to bring the stack down and up again. This costs roughly
// 30-60s per scenario in setup and 30s in teardown — well within the 30m
// nightly job timeout and within the suite-level ~15-20m wall budget
// stated in §13.2.
//
// Scenarios:
//
//	ci-fs-readfile-eio          → filesystem.read_file@v1 → FilesystemReadFileAvailabilityBurn
//	ci-git-remote-unreachable   → git.clone@v1            → GitCloneAvailabilityBurn
//	ci-http-connection-reset    → http.request@v1         → HttpRequestAvailabilityBurn
//	ci-shell-hang-cancel        → shell.exec@v1           → ShellExecCancellationRateBurn
//	ci-shell-panic              → shell.exec@v1           → ShellExecAvailabilityBurn
//	ci-persist-fail             → http.request@v1 driver  → PersistAvailabilityBurn (global)
```

### Task 2.5: Verify build + vet

**Files:** none.

- [ ] **Step 1: Vet with e2e tag**

Run: `go vet -tags=e2e ./test/chaos/e2e/...`

Expected: no output.

- [ ] **Step 2: Build with e2e tag**

Run: `go build -tags=e2e ./test/chaos/e2e/...`

Expected: no output.

### Task 2.6: Update CHANGELOG with 0.5.0 entry

**Files:**
- Modify: `CHANGELOG.md` (insert a new section at the top, between the header and the `[0.4.0]` section)

- [ ] **Step 1: Add the 0.5.0 section**

Insert after line 5 (the `The format follows ...` line) and before the existing `## [0.4.0] — 2026-04-28` line:

```markdown
## [0.5.0] — 2026-04-29

Phase 2C.4 sub-project G — cancellation-rate + persist-availability SLOs. First sub-project of the operational-readiness track. Adds 8 per-Phase-1-capability cancellation-rate SLOs and 1 global persist-availability SLO. Re-enables the two chaos scenarios skipped at `v0.4.0` (`ci-shell-hang-cancel`, `ci-persist-fail`); nightly comprehensive returns to 6/6 active. No runtime Go code changes; no metric contract changes; no alertmanager changes. Spec-complete against `docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md`.

### Added

- 8 cancellation-rate SLOs in `ops/slo/{shell,git,filesystem,http}.yaml` — one per Phase 1 capability. PROVISIONAL initial objective: 99.0% (cancellation rate <1%). Burn-rate alerts: `ShellExecCancellationRateBurn`, `GitStatusCancellationRateBurn`, `GitCloneCancellationRateBurn`, `GitDiffCancellationRateBurn`, `GitCommitCancellationRateBurn`, `FilesystemReadFileCancellationRateBurn`, `FilesystemWriteFileCancellationRateBurn`, `HttpRequestCancellationRateBurn`.
- 1 persist-availability SLO in `ops/slo/persist.yaml` (NEW file). Global, no `capability` label. PROVISIONAL initial objective: 99.9% (persist failure rate <0.1%). Burn-rate alert: `PersistAvailabilityBurn`. SLI denominator derived from `runtime_adapters_execution_total + runtime_adapters_receipt_persist_failures`, exact via the RecordExecution-after-persist invariant established post-B8/B9.
- Test SLO mirrors under `ops/slo/test/` for the chaos canary stack (`service: "runtime-adapters-chaos-test"`, 5m period).
- Sloth-rendered recording rules + alerts under `ops/prometheus/generated/` (prod) and `ops/prometheus/generated/test/` (test).
- `Test_RecordExecution_OnlyAfterPersistSuccess` contract test in `internal/application/services/execute_service_test.go` — protects the persist SLI denominator derivation against future refactors of `execute_service.go` that would silently invalidate the SLO math. Failure messages explicitly point at the SLI file. (A2C4G.1.)

### Changed

- `test/chaos/e2e/comprehensive_test.go` — `ci-shell-hang-cancel` and `ci-persist-fail` rows un-skipped. The first now expects `ShellExecCancellationRateBurn`; the second expects `PersistAvailabilityBurn` (with `canonicalCap: ""` because the SLO is global). File-header doc updated to "All six CI scenarios are active as of v0.5.0".

### Notes

- Initial objectives ship PROVISIONAL — operator hypotheses, not yet calibrated against real load. Recalibration with evidence is a follow-up if production signal demands. (NG1.)
- No metric contract change. The existing inhibition rule `equal: ['sloth_slo', 'capability']` in `ops/alertmanager/alertmanager.yaml` is unchanged — it already handles both labeled and unlabeled SLOs correctly because Alertmanager treats absent capability on both source and target as equal.
- Real receivers (PagerDuty, Slack, Linear) for the new alerts ship with sub-project A+B (`v0.8.0`).

```

### Task 2.7: Commit B2 changes

**Files:** all modified above.

- [ ] **Step 1: Stage**

Run:
```bash
git add test/chaos/e2e/comprehensive_test.go CHANGELOG.md
```

- [ ] **Step 2: Commit**

Run:
```bash
git commit -m "$(cat <<'EOF'
feat(chaos): re-enable shell-hang-cancel + persist-fail in comprehensive (B2)

Phase 2C.4 / G — second bundle (closes G).

The two scenarios that shipped skipped under v0.4.0 are re-enabled
against the new SLOs introduced in B1:
  - ci-shell-hang-cancel  → ShellExecCancellationRateBurn
  - ci-persist-fail       → PersistAvailabilityBurn (global SLO,
                            canonicalCap: "" intentionally)

File-header doc updated: "All six CI scenarios are active as of v0.5.0".
The skipped-with-clear-note block is removed entirely; the scenarios
table reflects the new alert assignments.

CHANGELOG 0.5.0 entry added describing the full G delivery (cancellation
SLOs + persist SLO + contract test + chaos re-enable). Notes section
calls out that initial objectives are PROVISIONAL and that real
receivers come with A+B (v0.8.0).

Refs: docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md
      docs/phase-2c.4-roadmap.md §G
      D2C4G.7, D2C4G.8
EOF
)"
```

- [ ] **Step 3: Push**

Run: `git push`

### Task 2.8: Open B2 PR, await CI green, merge

**Files:** none.

- [ ] **Step 1: Open the PR**

Run:
```bash
gh pr create --title "feat: Phase 2C.4 / G B2 — re-enable chaos scenarios + CHANGELOG 0.5.0 (closes G)" --body "$(cat <<'EOF'
## Summary

Second and final bundle of sub-project G under Phase 2C.4. Re-enables the two chaos scenarios skipped at `v0.4.0` against the SLOs introduced in B1. Adds the CHANGELOG 0.5.0 entry.

## Files changed

| Area | Change |
|---|---|
| `test/chaos/e2e/comprehensive_test.go` | Un-skip `ci-shell-hang-cancel` (→ `ShellExecCancellationRateBurn`), un-skip `ci-persist-fail` (→ `PersistAvailabilityBurn`, global). File-header doc updated. |
| `CHANGELOG.md` | +0.5.0 section summarising the full G delivery (B1 + B2). |

## Spec / plan

- Spec: `docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md`
- Plan: `docs/superpowers/plans/2026-04-29-phase-2c.4-g-cancellation-persist-slos.md` Bundle 2

## Test plan

- [ ] `lint + unit + contract` green
- [ ] `observability` green
- [ ] `chaos-canary-e2e` green (canary unaffected — runs only `ci-http-connection-reset`)
- [ ] `integration` green
- [ ] Other CI jobs green
- [ ] After merge: closure ops step (manual nightly comprehensive run + tag `v0.5.0`)

Closes G when merged + `v0.5.0` tag pushed.
EOF
)"
```

- [ ] **Step 2: Watch CI**

Run: `gh pr checks $(gh pr view --json number --jq .number) --watch`

Expected: 9/9 green.

- [ ] **Step 3: Merge**

Run: `gh pr merge $(gh pr view --json number --jq .number) --merge --delete-branch`

Expected: branch deleted on origin.

- [ ] **Step 4: Sync local main**

Run: `git checkout main && git pull`

Expected: local main FF'd to the merge commit.

---

## Closure — manual nightly verification + tag `v0.5.0`

Goal: trigger a manual nightly comprehensive run to verify the 6/6 active scenarios pattern works end-to-end with the new SLOs, then push the `v0.5.0` tag if green.

### Task C.1: Trigger manual nightly run

**Files:** none.

- [ ] **Step 1: Dispatch the chaos-nightly workflow manually**

Run: `gh workflow run chaos-nightly.yaml`

Expected: command returns silently; a new run shows up in `gh run list --workflow=chaos-nightly --limit 1`.

- [ ] **Step 2: Capture the run ID**

Run: `RUN_ID=$(gh run list --workflow=chaos-nightly --limit 1 --json databaseId --jq '.[0].databaseId') && echo "Run ID: $RUN_ID"`

Expected: numeric run id printed.

- [ ] **Step 3: Watch the run** (it takes ~7-10 min for the comprehensive suite)

Run: `gh run watch "$RUN_ID" --exit-status`

Expected: SUCCESS. If FAIL, follow the spec's STOP rule — do NOT modify the test, do NOT change the budget, do NOT relax labels; document the failure and decide whether reactive bundle is needed.

- [ ] **Step 4: Verify per-job results**

Run: `gh run view "$RUN_ID" --json conclusion,jobs --jq '{conclusion, jobs: [.jobs[] | {name, conclusion}]}'`

Expected: top-level conclusion `success`; the single `chaos-comprehensive` job conclusion `success`.

- [ ] **Step 5: Verify no auto-issue created** (success path doesn't fire `if: failure()` step)

Run: `gh issue list --label chaos-nightly-fail --state all --limit 1 --json number,createdAt --jq '.[] | select(.createdAt > "2026-04-29T00:00:00Z")'`

Expected: empty (no new issues filed since v0.5.0 work began). If a stale issue from before exists, that's fine — only filter on dates after this work started.

### Task C.2: Tag `v0.5.0`

**Files:** none.

- [ ] **Step 1: Capture the merge commit of B2**

Run: `MERGE_SHA=$(git log --oneline --merges main | head -1 | awk '{print $1}') && echo "Tag target: $MERGE_SHA"`

Expected: a SHA. Verify it's the B2 merge commit by running `git show --stat "$MERGE_SHA"` and confirming the diff includes `test/chaos/e2e/comprehensive_test.go` and `CHANGELOG.md`.

- [ ] **Step 2: Create annotated tag**

Run:
```bash
git tag -a v0.5.0 "$MERGE_SHA" -m "$(cat <<'EOF'
Phase 2C.4 / G — cancellation-rate + persist-availability SLOs

Closes sub-project G of Phase 2C.4 (operational readiness track).

- 8 cancellation-rate SLOs (1 per Phase 1 canonical capability),
  per-capability label, PROVISIONAL objective 99.0% (cancellation
  rate < 1%)
- 1 persist-availability SLO, global, PROVISIONAL objective 99.9%
  (persist failure rate < 0.1%); SLI denominator derived from
  execution.total + receipt.persist.failures via the
  RecordExecution-after-persist invariant
- Contract test Test_RecordExecution_OnlyAfterPersistSuccess that
  protects the SLI denominator derivation against future refactors
- Re-enables ci-shell-hang-cancel + ci-persist-fail in
  test/chaos/e2e/comprehensive_test.go; nightly comprehensive
  returns to 6/6 active scenarios

No runtime Go code changes. No metric contract changes. No alertmanager
changes. Pure YAML + Sloth render + one _test.go file.

Refs: docs/superpowers/specs/2026-04-29-phase-2c.4-g-cancellation-persist-slos-design.md
      docs/phase-2c.4-roadmap.md §G
      D2C4G.1-8, A2C4G.1
EOF
)"
```

- [ ] **Step 3: Push the tag**

Run: `git push origin v0.5.0`

Expected: `* [new tag] v0.5.0 -> v0.5.0` in the push output.

- [ ] **Step 4: Verify tag landed on origin**

Run: `git ls-remote --tags origin v0.5.0`

Expected: a single line with the tag SHA + `refs/tags/v0.5.0`.

### Task C.3: Update roadmap status table

**Files:**
- Modify: `docs/phase-2c.4-roadmap.md` (the §9 status table — change G's row from `not started` to `closed (v0.5.0, 2026-04-29)`)

- [ ] **Step 1: Edit the row**

Find the line in the §9 status table that reads:

```
| G | `v0.5.0` | not started | — |
```

Replace with:

```
| G | `v0.5.0` | closed | 2026-04-29 |
```

- [ ] **Step 2: Commit + push directly to main** (matches existing roadmap-as-nav-doc convention)

Run:
```bash
git add docs/phase-2c.4-roadmap.md
git commit -m "docs(roadmap): mark G closed at v0.5.0 (2026-04-29)"
git push
```

Expected: roadmap status table reflects G's closure.

---

## Self-review (run mentally before declaring plan ready)

### Spec coverage

| Spec § | Plan task(s) |
|---|---|
| §1 Summary | implicit across all bundles |
| §2 G1 (cancellation SLOs) | Tasks 1.2-1.5 (prod) + 1.7 (test) |
| §2 G2 (persist SLO) | Tasks 1.6 (prod) + 1.8 (test) |
| §2 G3 (test variants) | Tasks 1.7-1.8 |
| §2 G4 (chaos re-enable) | Tasks 2.2-2.4 + Closure Task C.1 |
| §2 G5 (contract test) | Task 1.10 |
| §2 G6 (CHANGELOG + tag) | Task 2.6 + Closure Task C.2 |
| §3 P1-P6 (design principles) | implicit; Task 1.10 directly defends P3+P4 |
| §4.1 cancellation YAML | Tasks 1.2-1.5 |
| §4.2 persist YAML | Task 1.6 |
| §5 test variants | Tasks 1.7-1.8 |
| §6.1-6.4 chaos re-enable | Tasks 2.2-2.4 |
| §6.3 alertMatches verified | already verified during spec self-review; reflected in Task 2.3 row |
| §7 contract test | Task 1.10 (full code shown) |
| §8 PROVISIONAL header | Tasks 1.2-1.6 (all add the header annotation) |
| §9 OOS | implicit; nothing in plan touches OOS items |
| §10 D2C4G.1-8 | covered by tasks; commit messages cite |
| §11 A2C4G.1 | Task 1.10 (the contract test docstring is explicit per A2C4G.1) |
| §12 Bundle preview | matches B1+B2 as actually written (combined preview's B3+B4 into Closure section since both are post-PR) |
| §13 References | tags + commit messages + spec back-pointers |

No spec gaps.

### Placeholder scan

- No "TBD" / "TODO" / "implement later" / "fill in details" anywhere in tasks (the `t.Skip` placeholders in Task 1.10 Step 2 are explicitly replaced in Step 3 as the next step in the same task; that pattern is the implementer's natural workflow, not a forgotten gap).
- No "Add appropriate error handling" / "add validation" / "handle edge cases" without specifics.
- No "Write tests for the above" without actual test code.
- No "Similar to Task N" — every task has its full content (cancellation SLOs are repeated verbatim across capabilities to honor "the engineer may be reading tasks out of order").
- Steps that change code show the code.

### Type / signature consistency

- Counter names consistent: `runtime_adapters_execution_total` (Prom name; appears in all SLI selectors); `runtime_adapters.execution.total` (OTel name; appears in contract test snapshot).
- `runtime_adapters_receipt_persist_failures` (Prom, no `_total` suffix — verified against the dashboard's existing query in the spec); `runtime_adapters.receipt.persist.failures` (OTel name).
- Alert names: PascalCase consistent across cancellation SLOs (`ShellExecCancellationRateBurn`, `GitStatusCancellationRateBurn`, `GitCloneCancellationRateBurn`, `GitDiffCancellationRateBurn`, `GitCommitCancellationRateBurn`, `FilesystemReadFileCancellationRateBurn`, `FilesystemWriteFileCancellationRateBurn`, `HttpRequestCancellationRateBurn`).
- `PersistAvailabilityBurn` consistent in spec, plan, test row, CHANGELOG.
- SLO names: kebab-case consistent (`shell-exec-cancellation-rate`, `persist-availability`, etc.) — used in `sloName` test fields, `name:` YAML fields, and SLI descriptions.
- `comprehensive_test.go` row field names match the existing struct (verified against the file in B6/B7/B10).

No type drift.

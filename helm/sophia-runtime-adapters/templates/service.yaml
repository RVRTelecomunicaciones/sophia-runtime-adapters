apiVersion: v1
kind: Service
metadata:
  name: {{ include "sophia-runtime-adapters.fullname" . }}
  labels:
    {{- include "sophia-runtime-adapters.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sophia-runtime-adapters.selectorLabels" . | nindent 4 }}

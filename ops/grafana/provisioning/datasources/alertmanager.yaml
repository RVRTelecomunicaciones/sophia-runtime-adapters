apiVersion: 1

# Alertmanager datasource — required by the Alert list panel on
# runtime-overview (§10.9).

datasources:
  - name: Alertmanager
    uid: alertmanager
    type: alertmanager
    access: proxy
    url: http://alertmanager:9093
    editable: false
    jsonData:
      implementation: prometheus

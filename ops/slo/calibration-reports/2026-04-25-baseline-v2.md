# Calibration report — baseline v2

**Date:** 2026-04-25
**Author:** Russell
**Git SHA:** 60508fa24e6727ebffc9185a62f0bc18efd1c403
**Track:** Phase 2C.2
**Run duration:** 45.00081739998333 min

## Envelope

### Host

- Machine: Apple M1 Pro
- OS: Darwin 25.2.0
- Docker version: 29.0.1
- Compose version: 2.40.3-desktop.1
- Total host CPU: 8
- Total host RAM: 16.0 GiB

### Services under compose during this run

| Service | Image | CPU limit | Mem limit | Mem reservation |
|---|---|---|---|---|
| runtime-adapters | sha256:1ae3a4f01b70 | 2.0 | 2 GiB | 1 GiB |
| postgres | postgres:15-alpine | 1.0 | 512 MiB | — |
| otel-collector | otel/opentelemetry-collector-contrib:0.106.1 | 0.5 | 256 MiB | — |
| prometheus | prom/prometheus:v3.11.2 | 1.0 | 1 GiB | — |
| grafana | grafana/grafana:11.3.0 | 0.5 | 512 MiB | — |
| http-upstream-mock | nginx:1.27-alpine | 0.5 | 256 MiB | — |
| k6 | grafana/k6:0.58.0 | 1.0 | 512 MiB | — |

### cgroup verification (verify-limits.sh output)

```json
{
  "NanoCpus": 2000000000,
  "Memory": 2147483648,
  "MemoryReservation": 1073741824
}
```

### Metrics pipeline

runtime → OTLP gRPC → otel-collector (prometheus exporter :8889) ← scrape (5s) ← prometheus

Pull-based (ADR 0007). `prometheusremotewrite` NOT in path.

### Tool versions

| Tool | Version |
|---|---|
| k6 | 0.58.0 |
| sloth | 0.16.0 |
| promtool | 3.11.2 |
| otel-collector-contrib | 0.106.1 |
| Go | 1.26.2 |

---

## Core tier — full calibration


### shell.exec@v1 — core calibration

#### Baseline distribution

| metric | observed | current target | proposed target | decision | justification |
|---|---|---|---|---|---|
| p50 latency | 3.777167 ms | — | — | — | — |
| p95 latency | 7.647417 ms | — | — | — | — |
| p99 latency | 48.17125 ms | 5000 ms (`le=5`) | 250 ms (`le=0.25`) | **TIGHTEN** | 5.2× headroom over measured p99. `le=0.25` already in `obs.DurationBuckets` — no histogram boundary additions; existing calibration data remains valid. |

#### Evidence sources

- `evidence/2026-04-25-baseline-v2/summary.json` — k6 `handleSummary` raw
- `evidence/2026-04-25-baseline-v2/shell-exec-v1-baseline-prom-instant.json` — PromQL instant query at run-end
- `evidence/2026-04-25-baseline-v2/shell-exec-v1-baseline-prom-range.json` — PromQL query_range over run window

### filesystem.read_file@v1 — core calibration

#### Baseline distribution

| metric | observed | current target | proposed target | decision | justification |
|---|---|---|---|---|---|
| p50 latency | 3.607166 ms | — | — | — | — |
| p95 latency | 6.264125 ms | — | — | — | — |
| p99 latency | 15.752542 ms | 500 ms (`le=0.5`) | 100 ms (`le=0.1`) | **TIGHTEN** | 6.3× headroom over measured p99. `le=0.1` already in `obs.DurationBuckets`. Tightest of the 4 core targets — read path is the hottest and most consistent. |

#### Evidence sources

- `evidence/2026-04-25-baseline-v2/summary.json` — k6 `handleSummary` raw
- `evidence/2026-04-25-baseline-v2/filesystem-read_file-v1-baseline-prom-instant.json` — PromQL instant query at run-end
- `evidence/2026-04-25-baseline-v2/filesystem-read_file-v1-baseline-prom-range.json` — PromQL query_range over run window

### filesystem.write_file@v1 — core calibration

#### Baseline distribution

| metric | observed | current target | proposed target | decision | justification |
|---|---|---|---|---|---|
| p50 latency | 4.7139585 ms | — | — | — | — |
| p95 latency | 7.7686854 ms | — | — | — | — |
| p99 latency | 51.00866891999992 ms | 1000 ms (`le=1`) | 500 ms (`le=0.5`) | **TIGHTEN** | 9.8× headroom over measured p99. Originally proposed 300 ms (`le=0.3`) but 0.3 is not in `obs.DurationBuckets` and adding it would require code change + re-baseline; chose `le=0.5` (existing bucket) instead. Still 2× tighter than current PROVISIONAL. |

#### Evidence sources

- `evidence/2026-04-25-baseline-v2/summary.json` — k6 `handleSummary` raw
- `evidence/2026-04-25-baseline-v2/filesystem-write_file-v1-baseline-prom-instant.json` — PromQL instant query at run-end
- `evidence/2026-04-25-baseline-v2/filesystem-write_file-v1-baseline-prom-range.json` — PromQL query_range over run window

### http.request@v1 — core calibration

#### Baseline distribution

| metric | observed | current target | proposed target | decision | justification |
|---|---|---|---|---|---|
| p50 latency | 4.159125 ms | — | — | — | — |
| p95 latency | 7.115 ms | — | — | — | — |
| p99 latency | 130.679583 ms | 10000 ms (`le=10`) | 500 ms (`le=0.5`) | **TIGHTEN** | 3.8× headroom over measured p99. The longest tail of the 4 core (p99/p95 ratio = 18×) — driven by HTTP handshake/keepalive in early requests. `le=0.5` already in `obs.DurationBuckets`. |

#### Evidence sources

- `evidence/2026-04-25-baseline-v2/summary.json` — k6 `handleSummary` raw
- `evidence/2026-04-25-baseline-v2/http-request-v1-baseline-prom-instant.json` — PromQL instant query at run-end
- `evidence/2026-04-25-baseline-v2/http-request-v1-baseline-prom-range.json` — PromQL query_range over run window


---

## Git smoke tier — limited confidence

### git.status@v1 — SMOKE-CALIBRATED (limited confidence)

**Scenario:** smoke (5 rps × 90s) + saturation_lite (ramp 20→80 rps × 3m).

**Caveat:** target below is **calibrated with limited confidence**. Re-confirm in 2C.4 when a sustained git.status scenario is added to suite.js.

**Targeted standalone run** (90s smoke @ 5 rps, 451 samples, 2026-04-25, run separately from suite.js to obtain a clean filtered sub-metric):

| metric | observed | current target | proposed target | decision | justification |
|---|---|---|---|---|---|
| p50 latency | 8.0 ms | — | — | — | — |
| p95 latency | 14.2 ms | — | — | — | — |
| p99 latency | 21.8 ms | 2000 ms (`le=2`) | 250 ms (`le=0.25`) | **SMOKE_CALIBRATED** | 11× headroom over measured p99 (vs 4–6× used for core 4) reflecting limited confidence: only 90s @ 5 rps, no saturation evidence. p99 under sustained load may be higher. Still 8× tighter than current PROVISIONAL. `le=0.25` in `obs.DurationBuckets`. |
| max | 361.8 ms | — | — | — | Single outlier — likely cold-start / GC pause. |

**Clean vs dirty tree split:** **unavailable in this run.** k6 only emits filtered sub-metrics in `handleSummary` for tags that have a threshold reference. The targeted run threshold filtered by `phase:smoke,capability:git.status@v1` only — the per-tree (`tree=small-repo|dirty-tree`) breakdown is not extractable. This is a reporting limitation, not a measurement limitation; resolvable in 2C.4 by adding explicit per-tree thresholds.

---

## Git rough tier — observation only, NOT calibrated

**Status:** targets in `ops/slo/git.yaml` for `git.clone@v1`, `git.diff@v1`, `git.commit@v1` remain **PROVISIONAL / ROUGH** post-2C.2. **No YAML changes proposed.**

**Observed ranges:** **unavailable in this run.** `git_rough.js` ships with no thresholds (D2C2.9 — observation only). k6 only emits filtered sub-metrics for tags carrying a threshold reference, so `git-rough-observations.json` returned `null` for all three capabilities. This is a reporting-pipeline limitation, not a measurement limitation; future 2C.4 work could add observability-only thresholds without changing the calibration policy (rough remains PROVISIONAL).

Raw data file (currently null-valued): `evidence/2026-04-25-baseline-v2/git-rough-observations.json`.

**Decision for all three rough capabilities:** ROUGH_NO_CHANGE — `ops/slo/git.yaml` targets stay at their PROVISIONAL values:

| Capability | Current target | Decision |
|---|---|---|
| `git.clone@v1` | 30000 ms (`le=30`) | ROUGH_NO_CHANGE (D2C2.9) |
| `git.diff@v1` | 3000 ms (`le=3`) | ROUGH_NO_CHANGE (D2C2.9) |
| `git.commit@v1` | 2000 ms (`le=2`) | ROUGH_NO_CHANGE (D2C2.9) |

---

## Summary — SLO changes proposed

| File | SLO | Field | Before | After | Tier | Confidence | Decision |
|---|---|---|---|---|---|---|---|
| `shell.yaml` | shell-exec-latency | p99 `le` | `5` (5000 ms) | `0.25` (250 ms) | core | high | **TIGHTEN** |
| `filesystem.yaml` | filesystem-read-file-latency | p99 `le` | `0.5` (500 ms) | `0.1` (100 ms) | core | high | **TIGHTEN** |
| `filesystem.yaml` | filesystem-write-file-latency | p99 `le` | `1` (1000 ms) | `0.5` (500 ms) | core | high | **TIGHTEN** |
| `http.yaml` | http-request-latency | p99 `le` | `10` (10000 ms) | `0.5` (500 ms) | core | high | **TIGHTEN** |
| `git.yaml` | git-status-latency | p99 `le` | `2` (2000 ms) | `0.25` (250 ms) | smoke | limited | **SMOKE_CALIBRATED** |
| `git.yaml` | git-clone-latency | — | `30` (30000 ms) | `30` (30000 ms) | rough | n/a | ROUGH_NO_CHANGE |
| `git.yaml` | git-diff-latency | — | `3` (3000 ms) | `3` (3000 ms) | rough | n/a | ROUGH_NO_CHANGE |
| `git.yaml` | git-commit-latency | — | `2` (2000 ms) | `2` (2000 ms) | rough | n/a | ROUGH_NO_CHANGE |

**Counts (16 SLOs total: 8 latency in 2C.2 scope + 8 availability out of 2C.2 scope):**

- TIGHTEN (high confidence, calibrated from suite baseline):  **4** of 16 (`shell.exec@v1`, `filesystem.read_file@v1`, `filesystem.write_file@v1`, `http.request@v1`)
- SMOKE_CALIBRATED (limited confidence):                      **1** of 16 (`git.status@v1`)
- ROUGH_NO_CHANGE (deferred to 2C.4 per D2C2.9):              **3** of 16 (`git.clone@v1`, `git.diff@v1`, `git.commit@v1`)
- AVAILABILITY (out of 2C.2 scope, unchanged):                **8** of 16 (one availability SLO per capability)

**Histogram boundary impact:** zero. All new `le` values reuse existing buckets in `obs.DurationBuckets {0.1, 0.25, 0.5, 1, 2, 3, 5, 10, 30, 60}`. No code changes to `internal/infrastructure/obs/metrics.go`. Existing calibration data remains valid (no re-baseline required).

**Verification:** `TestDurationBuckets_CoverSlothThresholds`, `ops/slo/` (`-tags sloth`), and `ops/slo/calibration-reports/` (`-tags loadreport`) all pass.

---

## Evidence files

Stored in `evidence/2026-04-25-baseline-v2/`:

- `manifest.json` — machine-readable envelope metadata.
- `summary.json` — raw k6 `handleSummary(data)` output.
- `<capability>-baseline-prom-instant.json` — per-capability PromQL instant query at run-end.
- `<capability>-baseline-prom-range.json` — per-capability PromQL query_range over the run window.
- `git-status-smoke-split.json` — git.status metrics segmented by `tree=small-repo|dirty-tree`.
- `git-rough-observations.json` — rough tier raw data.

---

*This report was generated by `ops/load/lib/generate-report.sh`. Do not edit by hand — re-run the calibration to amend.*
